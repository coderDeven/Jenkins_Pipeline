pipeline {
    agent { label 'deven' }
    stages {
        stage('Stage One') {
            steps {
                echo "build iOS"
            }
        }
        stage('Stage getParam') {
            steps {
                // sh './iOS/Sdebug.sh' $game_id $push_enable $token
                echo "woskspace : ${WORKSPACE}"
                echo "gameid : ${params.game_id}"
                sh './iOS/Sdebug.sh ${params.game_id}' 
            }
        }
    }
}