pipeline {
    agent { label 'deven' }
    stages {
        stage('build iOS') {
            steps {
                echo "build iOS"
            }
        }
        stage('build android') {
            steps {
                echo "build android"
            }
        }
    }
}