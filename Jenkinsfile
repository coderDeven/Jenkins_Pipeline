pipeline {
    agent { label 'deven' }
    stages {
        stage('build iOS') {
            echo "build iOS"
        }
        stage('build android') {
            echo "build android"
        }
    }
}